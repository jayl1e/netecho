apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: netecho
  name: netecho
spec:
  replicas: 1
  selector:
    matchLabels:
      app: netecho
  template:
    metadata:
      labels:
        app: netecho
    spec:
      containers:
      - name: netecho
        image: lijie0123/netecho
        imagePullPolicy: Always
        ports:
        - containerPort: 8080
          name: http
          protocol: TCP
        resources:
          limits:
            cpu: 1
            memory: 128Mi
          requests:
            cpu: 1
            memory: 128Mi
        readinessProbe:
            httpGet:
              path: /
              port: http
              scheme: HTTP
            periodSeconds: 300
            successThreshold: 1
            failureThreshold: 6
            timeoutSeconds: 1
        livenessProbe:
            httpGet:
              path: /
              port: http
              scheme: HTTP
            periodSeconds: 600
            successThreshold: 1
            failureThreshold: 24
            timeoutSeconds: 1
        startupProbe:
            httpGet:
              path: /
              port: http
              scheme: HTTP
            periodSeconds: 1
            failureThreshold: 10
            timeoutSeconds: 1
      terminationGracePeriodSeconds: 30

---
apiVersion: v1
kind: Service
metadata:
  name: netecho
spec:
  ports:
  - name: http
    port: 80
    targetPort: http
  selector:
    app: netecho
